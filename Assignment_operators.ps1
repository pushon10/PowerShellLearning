#Defining the variable, making it equal 10
$var = 10

#Adding 5 to the variable and setting the new total to it
$var += 5
#Presenting the new value in var on-screen
Write-Host 'The new value in var is' $var
#Decreasing the value of var by 2
$var -= 2
#Presenting the new value in var on-screen
Write-Host 'The new value in var is' $var
$var *= 1
#Presenting the new value in var on-screen
Write-Host 'The new value in var is' $var
#Decreasing the value of var by 2
$var /= 2
#Presenting the new value in var on-screen
Write-Host 'The new value in var is' $var