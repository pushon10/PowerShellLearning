#The following command installs a module. '-Force' makes it so that it installs without any further prompts
#'-Scope CurrentUser' makes it so that this is only installed for the current user.

Install-Module PSReadline -Force -Scope CurrentUser